# Required Environment Variables for Vercel

# MongoDB connection string (hosted MongoDB Atlas or similar)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
